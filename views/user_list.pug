extends layout 

block content
    h1= title

    ul
        each user in user_list
            li 
                a(href=user.url) #{user.first_name} #{user.last_name}
                |  (#{user.email})

        else 
            li There are currently no users.